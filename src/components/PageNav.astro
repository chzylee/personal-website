---
import { Image } from 'astro:assets';
import photo from '../assets/photo.jpg';
---

<header class="page-nav">
  <a href="/" class="page-nav__left" aria-label="Go to home">
    <Image
      src={photo}
      alt=""
      width={24}
      height={24}
      class="page-nav__photo"
      aria-hidden="true"
    />
    <span class="page-nav__name">Noah Lee</span>
  </a>

  <nav class="page-nav__links" aria-label="Site navigation">
    <a href="/work" class="page-nav__link">Work</a>
    <a href="/blog" class="page-nav__link">Writing</a>
    <a href="/#photography" class="page-nav__link">Photography</a>
    <a
      href="/NoahLeeResume.pdf"
      target="_blank"
      rel="noopener noreferrer"
      class="page-nav__link"
    >Resume</a>
  </nav>
</header>

<style>
  .page-nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: var(--nav-height);
    background: rgba(14, 13, 11, 0.78);
    backdrop-filter: blur(14px) saturate(140%);
    -webkit-backdrop-filter: blur(14px) saturate(140%);
    border-bottom: 1px solid var(--color-rule);
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    padding: 0 2rem;
  }

  .page-nav__left {
    display: flex;
    align-items: center;
    gap: 0.875rem;
    color: var(--color-ink);
    transition: color 0.2s ease;
  }

  .page-nav__left:hover { color: var(--color-rust); }

  .page-nav__photo {
    width: 24px;
    height: 24px;
    border-radius: 50%;
    object-fit: cover;
    object-position: center;
  }

  .page-nav__name {
    font-family: var(--font-display);
    font-size: 1.25rem;
    letter-spacing: 0.04em;
    line-height: 1;
    color: inherit;
  }

  .page-nav__links {
    display: flex;
    align-items: center;
    gap: 1.75rem;
  }

  .page-nav__link {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-ink-muted);
    transition: color 0.2s ease;
    padding: 0.75rem 0;
  }

  .page-nav__link:hover { color: var(--color-rust); }

  @media (max-width: 640px) {
    .page-nav {
      gap: 1rem;
      padding: 0 1rem;
    }
    .page-nav__links { gap: 1rem; }
    .page-nav__link { font-size: 0.6875rem; letter-spacing: 0.06em; }
  }
</style>
